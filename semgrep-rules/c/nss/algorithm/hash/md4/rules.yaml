# Copyright © 2026 Scan Open Source Solutions SL (SCANOSS). All Rights Reserved.
# This rules defined in this file are proprietary and confidential. No part may be used, copied, reproduced, modified, distributed, or transmitted in any form without a valid written commercial license agreement with SCANOSS.
# Use of this software requires such an agreement. To obtain a license, visit scanoss.com

rules:
  - id: c.nss.algorithm.hash.md4
    message: Detected MD4 hash function usage
    mode: taint
    options:
      interfile: true
    severity: INFO
    languages: [c]
    pattern-sources:
      - patterns:
          - pattern: $MECH
          - metavariable-regex:
              metavariable: $MECH
              regex: '^SEC_OID_MD4$'
    pattern-sinks:
      - patterns:
          - pattern: $API($MECH)
          - metavariable-regex:
              metavariable: $API
              regex: '^PK11_CreateDigestContext$'
          - focus-metavariable: $MECH
    metadata:
      category: security
      subcategory: cryptography
      confidence: HIGH
      crypto:
        assetType: algorithm
        operation: "digest"
        algorithmPrimitive: hash
        algorithmFamily: MD4
        algorithmName: MD4
        algorithmParameterSetIdentifier: "128"
        library: NSS
        api: $API
